# sharoshi-worklog-mvp 設計資料（現状スナップショット）

作成日: 2026-06-18 ／ 作成: HLS（adachi）
対象: 社労士法人 人事オフィス・櫻井所長 向け「売上×工数 生産性分析ツール」MVP
位置づけ: 第4〜7章は案2着手前の現状仕様の記録、**第8章が案2の設計正本**。案2は **2026-06-23 に公開デモへ協調デプロイ済み**で、以後 **第8章が現行仕様**（第4〜7章は案2前の経緯記録として残す）。
最終更新: 2026-06-24（業務区分カタログ確定〔確定15コード・027=FBデータ作成費用/028=マイナンバー管理料金=配賦対象外・旧037/046/100廃止〕、デモデータ再構築〔8名・22社・2026-04〜06・平日分散・全工数メモ・複数明細月3〜4〕、`item_master`（品目マスタ）完全撤去、配賦対象外の表記統一〔旧「対象外（立替）」→「配賦対象外」〕、新画面「工数入力（月間）」追加とメニュー再編、メモ改行対応・スマホ非活性表示の改善を実装・デプロイ。GAS協調デプロイ＋FTPSで公開デモに反映済み。検証は `scripts/verify-demo.mjs`＋ライブ `bootstrap`/`dashboard` で実証）。前回 2026-06-23 案2を協調デプロイ。
関連: `../売上工数分析ツール_構想メモ_v1.md`（要件構想）, `../DEPLOY.md`（配置手順）

---

## 1. 目的とスコープ

請求業務そのものは既存SaaSを継続利用する前提で、「請求の結果（売上）＋工数」を集計し、次の指標を月次で自動可視化する分析ツール。請求書発行システムではない。

可視化する主要4指標（構想メモ準拠）:

1. クライアント別の時間単価（売上 ÷ 投下工数）
2. スタッフ別の時間単価（帰属売上 ÷ 稼働工数）
3. スタッフ別の月次売上（帰属売上）
4. スタッフ別の売上目標達成率（帰属売上 ÷ 目標）

現状のデモ版はFIX版ではない。本番までに新規実装する機能が残っており、デモにある画面・機能がそのまま最終形ではない。デモ未実装の本番実装スコープは第15章に集約する。

---

## 2. 全体アーキテクチャ

3層構成。サーバ側ロジックは持たず、静的フロント＋GAS＋スプレッドシートで完結する軽量構成。

- フロントエンド: HLSレンタルサーバ上の静的ファイル（HTML/CSS/JS）。ビルド工程なし。
- バックエンドAPI: Google Apps Script（`gas/Code.gs`）をウェブアプリとしてデプロイ。
- データストア: Googleスプレッドシート（9シート。案2で +3〔task_phase_master / customer_staff_master / app_settings〕、2026-06-24 に item_master 撤去 −1）。

通信方式は **JSONP**（`backend.js` が `<script>` タグを動的生成してGASを呼ぶ）。静的サイトからCORS制約なしで呼べる反面、後述のセキュリティ制約がある。

```
[ブラウザ: index/staff/dashboard]
        │  JSONP (?action=...&token=...&payload=...&callback=...)
        ▼
[GAS Web App: doc/Code.gs  doGet → route_]
        │  SpreadsheetApp
        ▼
[Google Spreadsheet: worklogs ほか9シート]
```

### データフロー
- 参照系: `bootstrap`（マスタ＋工数）, `dashboard`（集計用フルデータ）。
- 更新系: `saveEntry` / `saveEntries` / `deleteEntry` / `upsertMaster` / `removeMaster`。更新時はGAS側ダッシュボードキャッシュを無効化。

---

## 3. 画面構成

| 画面 | ファイル | 用途 |
|---|---|---|
| Topメニュー | `index.html`（自己完結・インラインCSS/JS） | 各画面への入口。目的別3グループ（日常の入力／分析・可視化／管理・設定）＋デバイスバッジ。簡易パスワードゲート付き（デモ用） |
| 工数入力（月間／PC） | `worklog-month.html` / `worklog-month.js` / `worklog-month.css` | スタッフ1名×1か月を「行=顧客×業務×工程・列=日・セル=工数」のマトリクスで俯瞰入力。担当顧客を上位シード・工程は担当役割で自動補完・前月構成コピー・顧客ブロックの区切り（名称は先頭行のみ・境界線・交互バンド）・行全体ホバー。**セルは表示専用で単一クリック→編集ウィンドウ**（時間＋メモ）。**複数明細セルは読取専用＋「スマホ用画面で編集」**で `staff.html` を当該日付・スタッフ・顧客/業務/工程プリセットで開く（`from=worklog-month`／戻る導線・対象月/スタッフ復元）。**日常の入力グループの主画面**（2026-06-24 新設） |
| 作業内容登録（モバイル） | `staff.html` / `staff.js` / `staff.css` | スタッフが現場で打つ簡易入力。時/分ステッパUI。メモは複数行可（textarea）。工数入力（月間）からの誘導時のみ上部に「工数入力（月間）に戻る」を表示し、日付・スタッフ・顧客/業務/工程をプリセット |
| 工数 照会・訂正（PC・管理者） | `worklog.html` / `app.js` / `styles.css` | 工数の照会・訂正。カレンダー（日別合計・日クリックで一覧を当日に絞り込み）、日次サマリー（折り畳み・**コード基準集計**・社員番号/顧客コード併記・行クリックで一覧フィルタ）、入力一覧（フィルタ・コード併記）。**入力/編集は編集モーダル**（一覧の編集ボタン／新規登録ボタン）、削除は確認モーダル。工数CSV入出力。**管理・設定グループ**（2026-06-24 移設／2026-06-25 照会・訂正へ再設計） |
| 経営ダッシュボード | `dashboard.html` / `dashboard.js` / `dashboard.css` | 月次KPI・推移・スタッフ別／顧客別の生産性分析 |
| マスタメンテナンス | `master.html` / `master.js` / `master.css` | スタッフ／顧客／業務区分・工程／顧客担当／設定の各マスタをタブ切替で編集（upsert/削除・検索）。品目（請求項目）タブは 2026-06-24 撤去（業務区分に一本化） |
| 売上目標登録 | `targets.html` / `targets.js` / `targets.css` | 対象月を選びスタッフ別の月次売上目標を一括入力。前月コピー・合計表示・一括保存 |
| 請求 照会・訂正（PC） | `data-edit.html` / `data-edit.js` / `data-edit.css` | **請求（billing）専念**（2026-06-25 工数タブ撤去）。請求データをExcelライクなグリッドで照会・一括修正（請求月/顧客フィルタ・行追加/削除・変更行のみ一括保存）。工数の訂正は「工数 照会・訂正」へ |
| 詳細分析 | `analysis.html` / `analysis.js` / `analysis.css` | 任意期間でスタッフ／顧客／業務区分を軸に工数を多角分析（月次推移スタック・ランキング/構成比・クロス集計ピボット）。売上系指標は案2後 |
| 共通 | `config.js` / `backend.js` | 接続設定とAPIクライアント |

`config.js` は接続情報（storageMode / apiBaseUrl / apiToken）。`storageMode: "local"` でローカル保存にフォールバック可能。

`index.html` は Topメニュー。カードを目的別3グループに整理し、デバイスバッジ（PC/スマホ）を付す。
- 日常の入力：作業内容登録（`staff.html`・スマホ）／工数入力（月間）（`worklog-month.html`・PC）
- 分析・可視化：経営ダッシュボード（`dashboard.html`）／詳細分析（`analysis.html`）
- 管理・設定：マスタメンテナンス（`master.html`）／売上目標登録（`targets.html`）／請求 照会・訂正（`data-edit.html`）／工数 照会・訂正（`worklog.html`）

各画面右上のジャンプボタンは **同カテゴリの他画面＋メニュー** に統一（2026-06-25。日常の入力は対象外）。**メニュー（ホーム）ボタンはフィル、横移動ボタンはソフト**で差別化（`.nav-home`）。

デモ用の簡易パスワードゲートを持ち、`sessionStorage` で開錠状態を保持する。パスワードは `index.html` 内の定数（平文・デモ専用）。本番認証は別メモ（[production-auth-db-memo.md](./production-auth-db-memo.md)）の方針による。

ダッシュボードには2つのドリルダウン・モーダルがある。

- スタッフ行クリック → スタッフ月次サマリ。当月のKPIと「全工数内訳（顧客＋社内/非生産工数・工数の多い順、社内は末尾）」を表示。
- 顧客行クリック → 顧客プロファイル。当月KPI（税抜売上/顧客工数/時間単価/12ヶ月平均単価/12ヶ月売上/12ヶ月工数）、時間単価の12ヶ月推移バーチャート、当月の関与スタッフ内訳、当月の請求内訳を表示。推移バー（列）をクリックするとモーダル内の表示月だけが切り替わる（ダッシュボードの対象月・12ヶ月ウィンドウは不変。データのない月は0表示）。

---

## 4. データモデル（スプレッドシート9シート）

GAS `CONFIG.headers` が正本。デプロイ済みスプレッドシート `joffice_wklog_demo` と一致を確認済み。案2で task_phase_master / customer_staff_master / app_settings を新設、2026-06-24 に item_master を撤去（→9シート）。

| シート | カラム |
|---|---|
| worklogs | id, date, staffCode, staff, customerCode, customer, taskType, hours, memo, updatedAt, **taskCode, phaseCode** |
| staff_master | code, name |
| customer_master | code, name |
| task_master（業務区分） | code, name, **allocationType**（service/excluded/tax） |
| task_phase_master（工程） | taskCode, phaseCode, phaseName, ratio(%), sortOrder |
| customer_staff_master（顧客担当・時系列） | customerCode, staffCode, role, sortOrder, **effectiveFrom**（YYYY-MM） |
| billing_data | invoiceId, billingMonth, customerCode, customer, invoiceItem, paymentMethod, netAmount, taxAmount, grossAmount, issuedDate, paymentDueDate, paymentStatus, memo, **invoiceItemCode, transferDate** |
| staff_target_master | targetMonth, staffCode, staff, targetAmount |
| app_settings | key, value |

補足:
- **`item_master`（品目＝請求項目マスタ）は 2026-06-24 撤去**。案2の恒等マッチング（`billing_data.invoiceItemCode` ＝ `task_master.code`）により請求項目コードの正本が業務区分マスタに移り、item_master は配賦・集計のどこからも参照されなくなったため。GAS の sheets/headers/MASTER_DEFS/bootstrap から除去し、`rebuildDemo()` でシートも削除（第8-2・第15-1）。
- 工数は「顧客直接（customerCodeあり）」と「社内/その他（customerCode空欄）」を区別する。`taskCode`＝業務コード、`phaseCode`＝工程（PRE/REV）。
- billing_data は請求結果（顧問先別・請求項目別の売上）。`netAmount`＝税抜。消費税(080)は税として独立行で保持（税抜売上には含めない）。
- staff_target_master はスタッフ×月の売上目標。app_settings は作業→請求オフセット等をキーバリューで保持。

### デモデータの現状（2026-06-25 顧客を受領実マスタへ更新）
- 期間: 2026年4月・5月・6月の3か月が工数・請求・目標すべてで揃っている。工数は各月の**平日へ分散**。
- スタッフ: S001〜S008 の8名。顧客: **受領「設計時確定顧客マスタ」由来の実顧客40社**（コード `0001`形式＝かつ・かいしゅう関与先コード基準。請求CSV 2026-03 に出現する40社に一致。名称はマスタ正本36社＋マスタ欠番4社 `0003/0009/0021/0023` は請求CSV由来）。**全工数にメモを付与**（実運用前提）。各月3〜4個の**複数明細セル**（同一 日×顧客×業務×工程に2件）を仕込み、工数入力（月間）の「複」保護を演示。
- 規模: 月間税抜売上（総）約 ¥3.6M〜¥4.4M（2026-04 ¥3,603,000 / 05 ¥3,778,000 / 06 ¥4,387,000）。配賦対象外（027/028/060）¥111,000/月、消費税は税抜の10%。売上目標は8名×¥480,000＝¥3.84M/月（達成率≒90〜111%）。
- 整合性: 各月とも「Σスタッフ帰属売上＝役務売上」で**未配賦ゼロ**。配賦区分・工程・時系列担当（顧客 0001 の Prepare を 2026-05 から S002 へ交代）も網羅。`scripts/demo-dataset.mjs`（Node参照実装）＋`scripts/verify-demo.mjs` が配賦エンジンで売上レンジ・整合・複数明細・全メモ・平日のみを検証（30/30）。生成は `gas/DemoSeed.gs` `rebuildDemo()`。
- 顧客コードが請求CSVと恒等一致するため、実請求CSV（2026-03）を取り込むと同コードでマッチし、当月（工数なし）は担当フォールバックで配賦される。

---

## 5. 指標の定義（dashboard.js: buildMonthModel / renderKpis）

> 注: 第5〜7章は**案2前の現状仕様の記録**。案2は 2026-06-23 にデプロイ済みで、**現行の指標定義・配賦は第8章が正本**（売上を役務/配賦対象外/税に分類、per-hour 系は工数対応売上、達成率は役務売上基準、主担当は顧客担当者マスタ由来 ほか）。本章は経緯把握用に残す。

### 全体KPI（対象月）
| KPI | 定義 |
|---|---|
| 税抜売上 | その月の billing_data の netAmount 合計（**全件**） |
| 総工数 | その月の worklogs の hours 合計 |
| 顧客業務比率 | 顧客直接工数 ÷ 総工数 |
| 顧客平均時間単価 | 税抜売上 ÷ 顧客直接工数 |
| 売上目標達成率 | 税抜売上 ÷ 目標合計 |

### スタッフ別
| 指標 | 定義 |
|---|---|
| 帰属売上 | 工数按分で配賦された売上（後述） |
| 目標 | staff_target_master のその月合計 |
| 達成率 | 帰属売上 ÷ 目標 |
| 顧客工数 | 顧客直接工数 |
| 総工数 | 顧客直接＋社内 |
| 直接時間単価 | 帰属売上 ÷ 顧客直接工数 |
| 総合生産性 | 帰属売上 ÷ 総工数 |

### 顧客別
| 指標 | 定義 |
|---|---|
| 税抜売上 | その顧客の netAmount 合計 |
| 顧客工数 | その顧客への直接工数合計 |
| 時間単価 | 税抜売上 ÷ 顧客工数 |
| 主担当 | **その月に最も工数を入れたスタッフ（工数から逆算）** |
| 請求内訳 | invoiceItem 別の売上 |

---

## 6. 配賦ロジック（現状：工数按分方式）

構想メモ4章の(2)工数按分方式を採用。月内で、顧客売上をその顧客に投下した工数比でスタッフへ配賦する。

擬似コード（dashboard.js buildMonthModel）:

```
for each customer:
    if (customer.hours == 0 || customer.revenue == 0): continue   // ★ここが論点
    for each staff who logged hours on this customer:
        staff.revenue += customer.revenue * (staff.hoursOnCustomer / customer.hours)
```

**前提条件**: 顧客に「工数」と「請求」の**両方**がある場合のみ配賦が発生する。

---

## 7. 既知の論点・制約

### 7-1. 配賦の非対称（最重要・改修対象）
全体KPIの「税抜売上」は billing_data を**全件合計**する一方、スタッフへの配賦は「工数と請求の両方がある顧客」に限られる。したがって**請求はあるが工数記録がゼロの顧問先**が存在する月では:

- その売上は総売上には算入されるが、どのスタッフの帰属売上にも乗らない（＝未配賦）。
- スタッフ帰属売上の合計 ＜ 全体の税抜売上。
- 全体達成率（税抜売上÷目標）＞ スタッフ別達成率の合計。
- 顧客平均時間単価は分子に工数ゼロ顧客の売上が入り分母には入らず、実態より高めに出る。
- その顧問先は顧客別表に「工数0h・時間単価¥0」で表示される。

社労士事務所では「顧問料は毎月請求するが当月は工数未記録」が普通に起こり得るため、本番データでは高確率で顕在化する。現デモデータでは該当ゼロのため未発生。

→ **改修方針（案2・第8章で設計確定）**: 工数の裏付けが無い売上（工数ゼロ顧客・工程未計上・マッピング漏れ）は **顧客担当者マスタへ等分フォールバック**配賦する。さらに紐づけを品目コード基準＋工程（Prepare/Review）に精緻化し、時間単価・生産性が歪まないよう売上を2系統（配賦売上／工数対応売上）に分ける。

### 7-2. 主担当の出どころ
現状の「主担当」は工数の最多入力者から逆算しているため、**工数ゼロの顧客では主担当を決定できない**。案2では **customer_staff_master（顧客担当者マスタ・複数担当可）** を新設し、表示・フォールバックともにこのマスタ由来とする（第8-8）。

### 7-3. しきい値ハードコード
顧客時間単価の色分け（rate>=10000:良 / >=7500:中 / 未満:低）、スタッフ達成率の色分け（>=1.0 / >=0.85）が dashboard.js にハードコード。所長の基準に合わせ調整余地あり。

### 7-4. セキュリティ
JSONP＋URLクエリにトークンを載せる方式で、`config.js` にトークンが平文。初回デモ用途では許容だが、本番は認証・権限・監査ログ・レート制限・編集履歴の設計が必要（DEPLOY.md 注記済み）。リポジトリでは `config.js` を .gitignore 対象とし、`config.sample.js` をプレースホルダとして管理する。

### 7-5. スタッフマスタの規模
（案2前の記録）seed-sample スクリプトは15名分、reseed-six は6名。**現行デモは `DemoSeed.gs rebuildDemo()` による8名・22社（2026-06-24 再構築）で整合**（第4章）。旧 seed スクリプトは参考用に残置。

---

## 8. 案2：工程対応・顧客担当者フォールバック配賦（実装済み・2026-06-23 デプロイ済み）

> 状態: 2026-06-23 に設計確定（受領資料 `J-Office_Insight_管理/【DM】請求品目一覧.xlsx`・`【マスク済】202603末〆.csv` 反映）後、**Phase1〜5を実装し同日 公開デモへ協調デプロイ済み**。
> 実装物: `gas/Code.gs`（新シート3・列追加・品目seed・API）、`gas/DemoSeed.gs`（`rebuildDemo()` で合成デモ再生成）、`allocation.js`（配賦エンジン・共有／`scripts/test-allocation.mjs` で回帰テスト）、ダッシュボード／詳細分析（売上フェーズ）／マスタ（工程・顧客担当・配賦区分・設定）／工数入力（工程）／data-edit（新列）。
> **2026-06-23 デプロイ済み**。GAS（clasp push＋`setup()`＋`rebuildDemo()`＝`scripts/deploy-an2.ps1`）とフロント（FTPS）を協調デプロイ済みで、公開デモ（`tools.h-linksystems.com/joffice/`）に反映されている（ライブ `bootstrap` で `taskPhases`/`customerStaff.effectiveFrom`/`tasks.allocationType`/`settings.billingOffset` を確認）。
> この章が**現行仕様の正本**（第4〜7章は案2前の経緯記録）。業務区分カタログは 2026-06-24 に確定（第8-12）し、旧「所長回答待ち」は解消済み（第8-14）。

### 8-1. 狙い

現状の配賦（第6章）は「顧客単位・総工数按分」で、業務区分を見ない。また請求はあるが工数ゼロの顧問先で未配賦が生じ、指標が歪む（第7-1）。案2では次を実現する。

- 売上↔工数の紐づけを **(請求月, 顧客, 業務コード)** の粒度に精緻化。
- すべての業務区分に **工程（Prepare / Review）の振分率**を持たせ、売上を工程比で分けてから工数按分。
- 工数の裏付けが無い役務売上は **顧客担当者マスタへ役割対応でフォールバック配賦**（役務売上は全額が必ず誰かに乗る）。
- 売上を性質で **役務売上 / 配賦対象外売上（立替・税）** に分類。
- 売上を集計用途で **配賦売上 / 工数対応売上** の2系統に分離し、金額系と per-hour 系で使い分け。

### 8-2. 売上↔工数の紐づけ（業務コード基準・恒等マッピング）

受領資料により、**請求側の「業務コード（品目コード）」がそのまま業務区分**であり、工程振分率・名称も同一表に載ることが判明した。したがって:

- 紐づけキーは **業務コード**。`billing_data.invoiceItemCode`（業務コード）＝ `task_master.code` ＝ `worklogs.taskCode` で**恒等マッチング**（別途のマッピング列は不要）。業務コードは外部「かつ・かいしゅう」で確定・運用されている正本。
- 紐づけは **(請求月→作業月変換, 顧客, 業務コード)**（作業月変換は第8-5）。
- マッチング漏れ（請求にあるが業務区分マスタに無い業務コード）の売上は、顧客担当者へフォールバック配賦し、**データ品質警告**（第8-11）。確定カタログ（第8-12）に無いコード（例 `000`（¥0行）/`057`）が該当。

### 8-3. 工程（Prepare / Review）モデル

- **すべての業務区分が工程振分率を持つ**（「工程なし」という分岐は設けない）。分割しない区分は **Prepare:100 / Review:0** としてマスタ登録する運用。
- 振分率は業務区分マスタ（実体は子＝工程マスタ）で管理。確定カタログの例：労務相談=100/0、給与計算=70/30、年末調整=70/30、賞与計算=70/30 など（第8-12）。Prepare/Review に限定せず **N工程・任意振分率（役務は合計100%）に拡張可能**な構造（当面2工程運用）。
- 工程は工数側に **別カラム `phaseCode`** で保持（業務区分名に内包しない）。工程入力UIは、ratio>0 の工程が1つだけなら自動選択/非表示にしてよい。

### 8-4. 配賦区分（役務 / 対象外）と消費税

業務区分マスタに **`配賦区分`** を持つ。0/0（Prepare:0/Review:0）の品目は性質で切り分ける。さらに**消費税は「税」であり売上ではない**ため、税抜売上からも除外する（3区分）。

| 区分 | 例 | 税抜売上(総) | 役務売上 | スタッフ配賦 |
|---|---|---|---|---|
| **役務**（配賦対象） | 給与計算026・労務相談001・スポット手続 ほか | 含む | 含む | する（工程比・工数登録対象・Σ振分率=100） |
| **配賦対象外**（立替・実費・手数料） | 諸費用060・FBデータ作成費用027・マイナンバー管理料金028 | **含む** | 含まない | しない（工数登録の選択肢に出さない・per-hour に入れない） |
| **消費税**（税） | 消費税080 | **含まない** | 含まない | しない（税込算出にのみ使用） |

- 消費税(080)は請求CSV上、**非080行(税抜)合計の10%として独立行**で入る（実データ 2026-03 で全社一致を確認・端数1社¥1差）。売上ではなく税なので**税抜売上にも含めない**。税込 = 税抜売上 ＋ 消費税。
- **027 FBデータ作成費用・028 マイナンバー管理料金は配賦対象外で確定（2026-06-24）**。確定カタログで両コードとも 0/0（工程比なし）＝役務にできないため配賦対象外。総売上には計上し、スタッフ配賦・役務売上・per-hour には乗らない（第8-12）。
- UI 表記は「**配賦対象外**」に統一（旧「対象外（立替）」。027/028 が加わり「立替」では実態に合わないため・2026-06-24）。配賦対象外売上は「捨てる」のではなく**名前を付けて別建て**にする（第8-7・第8-11）。

### 8-5. 請求月の決定と作業月オフセット（2つの別軸）

「ズレ」が2種類あり、**別物として両方持つ**。

**(ア) 振替日 → 請求月**（取込時に売上の帰属月を決める）
- 口座振替は翌月引落しのため、**請求月 = 振替日の前月（振替月 − 1・固定）**。例: 振替日 2026/4/22 → 請求月 2026-03。
- 1ファイル＝1請求月（振替日は単一）。取込時に自動付与し、**手修正可**。

**(イ) 作業月 → 請求月オフセット**（配賦時に対応工数の月を遡って探す・グローバル設定）
- 設定画面で **当月請求(0) / 翌月請求(+1) / 翌々月請求(+2)**。事務所一律（品目別は持たない）。
- マッチングは「**請求月 B の売上 ↔ 作業月 = B − オフセット の工数**」。
- 人事オフィスは工数も請求月と同月計上の想定のため **基本は (イ)=0**（工数データ確認後に確定）。
- 設定値は設定保持の仕組み（設定シート/専用マスタ）に1件保存。

### 8-6. 配賦アルゴリズム

```
オフセット = グローバル設定（0/1/2）
各 (請求月 B, 顧客, 業務コード I) について:
  R = その業務コードの税抜報酬額合計（消費税080は取込時に税として分離済み・配賦処理に入らない）
  T = code == I の業務区分（恒等マッチング）
  作業月 M = B − オフセット(イ)
  W = (作業月 M, 顧客, 業務区分 T) の工数エントリ群
  A(role) = 顧客担当者マスタ(顧客) の role 役割の担当者集合

  ■ T が無い（マッチング漏れ）:
      R を A(全役割) へ等分（フォールバック）＋警告

  ■ T.配賦区分 == 対象外（諸費用 等の立替・実費）:
      R は「配賦対象外売上」へ計上（スタッフ配賦なし・役務売上に含めない・税抜売上には含む）

  ■ T.配賦区分 == 役務:
      各工程 p（振分率 r_p, Σr_p=1。r_p=0 はスキップ）:
        枠 = R × r_p
        Wp = W のうち phaseCode == p
        Wp あり → 枠を Wp の工数比で按分（工数対応売上）
        Wp なし → 枠を「顧客の p 役割担当者」へ等分（フォールバック・役割対応）
        （p 役割担当者が未設定 → 未配賦として残し警告）
```

**売上2系統**（同じ配賦処理の結果を2通りに集計）:
- **配賦売上**（金額系：帰属売上・月次売上・達成率用）＝ 工数按分分 ＋ フォールバック分。合計 = **役務売上**。
- **工数対応売上**（per-hour 系：時間単価・生産性用）＝ 工数按分分のみ（**フォールバック分は除外**）。

**売上の階層**: 税抜売上(総) = 役務売上 ＋ 配賦対象外売上（立替）。Σスタッフ帰属売上 = 役務売上。税込 = 税抜売上(総) ＋ 消費税（080・税抜売上には含めない）。

### 8-6-1. 数値例

A社・当月（オフセット0=当月請求）。担当者: Prepare担当=P、Review担当=R。

| 業務コード | 金額 | 配賦区分 | 工程比 | 当月工数 |
|---|---|---|---|---|
| 026 給与計算 | ¥100,000 | 役務 | 70/30 | X:Prepare 4h, Z:Prepare 1h（Reviewなし） |
| 060 諸費用 | ¥20,000 | 対象外 | — | 工数なし |

- 給与計算 Prepare枠 ¥70,000 → X ¥56,000 / Z ¥14,000（工数対応）
- 給与計算 Review枠 ¥30,000 → Review工数ゼロ → **Review担当 R へ ¥30,000**（役割対応フォールバック）
- 諸費用 ¥20,000 → 対象外 → 誰にも載らない／役務売上に含めない

役務売上 = ¥100,000 = X56k + Z14k + R30k ✓（= Σ帰属売上）
総売上(税抜) = ¥120,000（役務 ¥100,000 ＋ 対象外 ¥20,000）
工数対応売上 = X56k, Z14k のみ（R のフォールバック分は除外）→ 時間単価 X = ¥14,000/h
→ R は工数ゼロでも帰属売上¥30,000・達成率に反映（時間単価には載らない）。

### 8-7. 指標の使い分け（売上系統マトリクス）

per-hour 系は **工数対応売上**、金額系は **配賦売上（=役務売上）** を用いる（全レベルで一貫）。配賦対象外（立替・税）は per-hour にも帰属売上にも入れず、総売上にのみ計上して別建て表示。

| 指標 | 使う売上 | フォールバック分 | 配賦対象外分 | 工数=0 の表示 |
|---|---|---|---|---|
| スタッフ 帰属売上（月次売上） | 配賦売上(役務) | **乗る** | 乗らない | — |
| スタッフ 達成率 | 配賦売上 ÷ 役務目標 | **押し上げる** | 乗らない | — |
| スタッフ 直接時間単価 | 工数対応売上 ÷ 直接工数 | 反映しない | 反映しない | 「—」 |
| スタッフ 総合生産性 | 工数対応売上 ÷ 総工数 | 反映しない | 反映しない | 「—」 |
| 顧客 売上 | 総売上（税抜・全額） | — | 含む（内訳表示） | — |
| 顧客 役務売上 | 役務売上 | 含む | 含まない | — |
| 顧客 時間単価 | 工数対応売上 ÷ 顧客工数 | 反映しない＋「未記録¥X」別表示 | 反映しない | 「—（工数未記録）」 |
| 全社 総売上(税抜) | 全額 | — | 含む | — |
| 全社 役務売上 | 役務のみ | 含む | 含まない | — |
| 全社 顧客平均時間単価 | 工数対応売上合計 ÷ 顧客直接工数合計 | 反映しない | 反映しない | — |
| 全社 達成率 | 役務売上 ÷ 役務目標 | 含む | 含まない | — |

判断確定（2026-06-23）:
- per-hour 系（時間単価・生産性）は**顧客個別も含め工数対応売上に統一**。顧客一覧で 売上列（総額）と「時間単価 × 工数」は一致しないため、差分（未記録分・対象外分）を別表示で補う。
- 「総合生産性」も**工数対応売上ベース**。
- **売上目標は「役務売上」基準**。達成率分母を役務売上に揃えることで Σスタッフ達成率 = 全社達成率 が一致する。

### 8-8. 工数なし売上の各指標への影響（要点・フォールバックと対象外の区別）

- **フォールバック分（役務だが工数ゼロ）**: 役務売上のまま担当者へ載る。帰属売上・達成率に**反映**。時間単価・生産性には**反映しない**。
- **配賦対象外分（立替・税）**: 誰にも載らず役務売上にも入らない。帰属売上・達成率・時間単価・生産性のいずれにも**反映しない**。総売上にのみ計上し別建て表示。
- **工数ゼロ月の顧客時間単価**は「—（工数未記録）」（¥0 でも ∞ でもない）。その売上は担当者フォールバックで全社役務売上・スタッフ達成率に反映。
- これにより第7-1 の不整合は解消する。「全社総売上 ≠ Σスタッフ帰属売上」が残るが、それは**配賦対象外（立替・税）ぶんの意図的な差**であり、別建て表示で説明できる（バグではない）。

### 8-9. データモデル変更

| マスタ/テーブル | 変更 | 主なカラム |
|---|---|---|
| task_master（業務区分） | 拡張 | `code`（業務コード＝請求項目コード）, `name`, `allocationType`＝配賦区分（service役務/excluded配賦対象外/tax税・新） |
| task_phase_master（工程） | **新設** | `taskCode`, `phaseCode`（例 PRE/REV）, `phaseName`, `ratio`（%）, `sortOrder`。役務は ratio 合計=100% をバリデーション。受領資料の Prepare/Review 列から取り込む |
| customer_staff_master（顧客担当者・**時系列**） | **新設** | `customerCode`, `staffCode`, `role`（=工程コード Prepare/Review。役割対応フォールバックの寄せ先）, `sortOrder`, `effectiveFrom`（**有効開始月 YYYY-MM**・2026-06-23追加） |
| worklogs | 拡張 | 既存に `taskCode`, `phaseCode` を追加 |
| billing_data | 拡張 | `invoiceItemCode`（業務コード）でコード恒等マッチング。`transferDate`（振替日）を保持し、`billingMonth`（請求月）= 振替月−1 を取込時に確定（手修正可）。消費税(080)は売上行にせず税として分離 |
| settings（設定） | **新設/拡張** | 作業→請求オフセット(イ)（0/1/2・グローバル）を1件保持 |

※ `task_master.code` が業務コードそのものなので、別途の `invoiceItemCode` 参照列は task 側には持たせない（恒等マッピング）。

### 8-10. 顧客担当者マスタ

- 1顧客に**複数担当者**を設定可能（仕組みとして保持）。人事オフィスは実データ上 **1顧客=Prepare担当1名＋Review担当1名の2名固定**運用（1スタッフは複数顧客の担当可）。
- `role` は**工程コード（Prepare/Review）**を指し、**役割対応フォールバック**の寄せ先になる（Review枠の未計上→Review担当へ）。
- ダッシュボードの「主担当」表示は、工数最多者からの逆算（現状・第5章顧客別）に代えて**本マスタ由来**に置き換える（工数ゼロ顧客でも提示可）。複数担当は役割つきで併記。

#### 8-10-1. 顧客担当の時系列化（有効開始月方式・2026-06-23）

各顧客の担当は**毎月変動しうる**ため、`customer_staff_master` を時系列マスタ化した（櫻井所長要件）。

- **保持方式**: 行に `effectiveFrom`（有効開始月 `YYYY-MM`）を持つ**差分保持**。担当が変わった月だけ行を追加する。
- **解決規則**: ある `(顧客, 役割, 対象月)` について `effectiveFrom ≤ 対象月` のうち**最新の `effectiveFrom`** の行を採用。`effectiveFrom` 空欄＝**初期から有効**（既存フラットデータの baseline。`YYYY-MM` 文字列比較で空が最小になる性質を利用）。`staffCode` 空欄＝**担当解除**（tombstone）。
- **参照月（全画面で統一）＝「作業月」**。配賦フォールバックは `請求月 − オフセット = 作業月` 時点で解決。入力画面は作業日の月、データ訂正画面は各行の日付の月で担当を判定（顧客プルダウンの担当上位表示・工程自動補完／不一致警告）。
- **解決ロジックの正本は `allocation.js`**：`resolveAssignees(rows, month)` / `roleAsOf` / `assigneesAsOf` を公開し、配賦エンジン・各画面・マスタUIが同一ロジックを共有する。
- **マスタUI（顧客担当タブ）**: 対象月(YYYY-MM)セレクタで**その月時点の担当を表示・編集**（＝年月指定の照会を兼ねる）。保存は**その月から有効**で、継承値と異なる役割のみ `effectiveFrom=対象月` で upsert（差分のみ保持。継承一致なら当月行は作らない）。「当月取消」で当月指定を消し前月以前へ戻す。`この月〜`／`引継` タグで由来を表示。
- **保存API**: `saveCustomerStaff` は `(顧客, 役割, 有効開始月)` で upsert（`staffCode` 空＝解除を許容）。`deleteCustomerStaff(customerCode, role, effectiveFrom)`。
- **デモ**: `DemoSeed.gs` が顧客 0001 の Prepare 担当を 2026-05 から交代させ（マスタ・工数を整合）、時系列の挙動を示す。回帰テスト（`scripts/test-allocation.mjs`）に時系列5件を常設。

### 8-11. 表示・データ品質

- 工数ゼロ顧客／工程の時間単価は「—（工数未記録）」。
- スタッフ詳細・顧客プロファイルに「**うち工数未記録の売上 ¥X**」、全社・顧客に「**総売上 / 役務売上 / 配賦対象外**」を別建て表示。
- 警告対象: マッチング漏れ業務コード（000/057 等）、顧客担当者・役割担当の未設定、役務なのに振分率合計≠100%、対象外なのに工数計上あり。

### 8-12. 業務区分マスタの確定カタログ（受領資料・2026-06-23 確定／2026-06-24 反映）

`【DM】請求品目一覧.xlsx`（業務コード・Prepare/Review比・名称・櫻井所長メール回答で確定）を業務区分マスタ／工程マスタの正本とする。`Code.gs TASK_CATALOG`（15コード）が実装の正本で、`rebuildDemo()` の `seedTaskCatalog_` が task_master/task_phase_master を再構築する。

| コード | 名称 | Prepare | Review | 配賦区分 |
|---|---|---|---|---|
| 001 | 労務相談 | 100 | 0 | 役務 |
| 002 | 事務長代行費用 | 100 | 0 | 役務 |
| 003 | 有給休暇管理費用 | 70 | 30 | 役務 |
| 026 | 給与計算 | 70 | 30 | 役務 |
| 027 | FBデータ作成費用 | 0 | 0 | 配賦対象外 |
| 028 | マイナンバー管理料金 | 0 | 0 | 配賦対象外 |
| 036 | スポット手続 | 100 | 0 | 役務 |
| 056 | 賞与計算 | 70 | 30 | 役務 |
| 060 | 諸費用 | 0 | 0 | 配賦対象外 |
| 061 | 給与支払報告書 | 70 | 30 | 役務 |
| 062 | 算定基礎届 | 70 | 30 | 役務 |
| 063 | 労働保険年度更新 | 70 | 30 | 役務 |
| 064 | 住民税変更 | 70 | 30 | 役務 |
| 065 | 年末調整 | 70 | 30 | 役務 |
| 080 | 消費税 | 0 | 0 | 税 |

確定に伴う変更（2026-06-24）: 旧 037/046/100 を**廃止**（スポット手続は **036 に一本化**）、旧 027「マイナンバー管理」は **028 マイナンバー管理料金へ移動**し 027 は **FBデータ作成費用**に。0/0 の 027/028/060 は配賦対象外、080 は税。`000/057`（旧「正体不明」）は確定カタログに無く＝出現すればマッチング漏れ警告（第8-11）。

### 8-13. デモデータ再構築（2026-06-24 実施）

`gas/DemoSeed.gs` `rebuildDemo()` が確定カタログに整合する合成データを再生成する（第4章「デモデータの現状」参照）。要件: 2026-04〜06／工数は平日分散／月間税抜売上 約360〜440万（**顧客40社＝受領実マスタ・0001形式**・スタッフ8名）／全工数にメモ／複数明細セル月3〜4個／顧客担当（Prepare/Review各1名・顧客0001は2026-05から交代）／目標8名×48万。Node 参照実装 `scripts/demo-dataset.mjs` ＋検証 `scripts/verify-demo.mjs` が配賦エンジンで「役務売上 = Σ帰属売上」「総売上 = 役務＋配賦対象外」「未配賦0」「売上レンジ」「複数明細3〜4」「全メモ」「平日のみ」を確認済み（30/30）。Code.gs カタログと参照実装の一致も照合。
- **顧客マスタの正本化（2026-06-25）**: 受領 `【DM】顧客マスタ.xlsx`「設計時確定顧客マスタ」（かつ・かいしゅう関与先コード `0001`形式・55社）を顧客マスタの正本とする。デモは請求CSV 2026-03 に出現する40社を採用（コードは請求CSVと恒等一致＝CSV取込が同コードでマッチ）。名称はマスタ正本36社＋マスタ欠番4社 `0003/0009/0021/0023` は請求CSV由来。**要所長確認**: 当4社がマスタ欠番である理由、および `0009≒0042`・`0021≒0038` の同名別コード（別請求口座の可能性）。

### 8-14. 確定事項・将来拡張

解決済み（2026-06-24 確定カタログ・第8-12）:
- `027 FBデータ作成費用` / `028 マイナンバー管理料金`: ともに 0/0 ＝**配賦対象外**で確定（スタッフ配賦せず・総売上にのみ計上）。
- スポット手続: **036 に一本化**（旧 037/028スポット は廃止・028はマイナンバーへ）。旧 046 就業規則・100 LAI も廃止。
- `000 / 057`: 確定カタログに無し＝正規の業務コードではない。請求CSVに出れば ¥0行はスキップ、それ以外はマッチング漏れ警告（第8-11）。

解決済み（2026-06-23 受領CSVで確認）:
- `080 消費税`: 請求CSV上、非080行(税抜)合計の**10%として独立行**で入る。**売上ではなく税**として扱い、税抜売上には含めず税込算出にのみ使用（第8-4）。

将来拡張:
- 工程は当面 Prepare/Review 2固定だが、工程マスタ追加で N 工程に拡張可能とする。
- 振分率・オフセットは固定/グローバル（顧客別・期間別・品目別は対象外。必要時に再設計）。
- 業務コード↔業務区分は恒等（1:1）。多対1は対象外。

---

## 9. キャッシュ戦略

- GAS側: `dashboard` 結果を ScriptCache に gzip+base64 で保存（既定300秒、95KB超は保存スキップ）。更新系API実行時に無効化。
- ブラウザ側: dashboard データを localStorage にキャッシュ（キー `worklog-dashboard-cache-v2`、TTL 30分、apiBaseUrl が変わると破棄）。初回は前回キャッシュを即表示し裏で更新する。

---

## 10. API一覧（GAS doGet → route_）

| action | 種別 | 内容 |
|---|---|---|
| bootstrap | 参照 | staff/customers/tasks/taskTypes/taskPhases/customerStaff/settings/entries（item_master 撤去により items は返さない・2026-06-24） |
| dashboard | 参照 | 集計用フルデータ（staff/customers/entries/billing/targets） |
| saveEntry / saveEntries | 更新 | 工数の登録・更新（id必須・upsert） |
| deleteEntry | 更新 | 工数削除 |
| saveBilling / saveBillings | 更新 | 請求データの登録・更新（invoiceId必須・upsert／一括） |
| deleteBilling | 更新 | 請求データ削除 |
| saveTarget / saveTargets | 更新 | スタッフ別売上目標の登録・更新（targetMonth＋staffCodeで特定／一括） |
| deleteTarget | 更新 | スタッフ別売上目標削除 |
| upsertMaster | 更新 | マスタupsert。対応typeは **staff/customers/tasks の3種**（`MASTER_DEFS`基準。oldCode指定でコード変更可。item_master 撤去で items は対象外・2026-06-24）。工程・顧客担当・設定は専用API |
| removeMaster | 更新 | マスタ削除（staff/customers/tasks） |

認証: `CONFIG.apiToken` と一致しないトークンは拒否。排他: 参照・更新とも LockService でスクリプトロック。

---

## 11. セットアップ／デプロイ

詳細は `../DEPLOY.md` を参照。要点:
1. スプレッドシート新規作成 → Apps Scriptに `gas/Code.gs` を貼付 → `setup()` 実行でシート生成。
2. ウェブアプリとしてデプロイし、発行URLを取得。
3. `config.js` に apiBaseUrl と apiToken を設定（リポジトリ管理外。`config.sample.js` を複製して作成）。
4. 静的ファイル一式をレンタルサーバの同一ディレクトリへ配置（`gas/` `docs/` `scripts/` はサーバ配置不要）。

### FTPSデプロイ（WinSCP / Windows）
- `scripts/deploy.ps1`：WinSCP で FTPS(explicit) 接続し、Webファイルのみをサーバへ配信。`-Only a.js,b.css` で変更ファイルのみ配信可。
- 認証情報は `scripts/deploy.config.ps1`（`.gitignore` 対象。`deploy.config.sample.ps1` を複製して作成）に保持。
- 配置先 `RemoteDir` は `/tools.h-linksystems.com/joffice/`（FTPルート直下にドメイン別フォルダがある構成。`public_html` は無い）。
- `scripts/ftp-ls.ps1`：リモートのディレクトリ調査用ヘルパー。

### git 運用
- 本フォルダは OneDrive 同期配下のため、Cowork サンドボックス（Linux）からは git 実行不可（マウントが unlink 不可）。git 操作は Windows 側で行う。
- `scripts/git-init.ps1`：初期化＋現状コミット（冪等。既存リポジトリなら追記コミットのみ）。
- `scripts/git-push-github.ps1`：ブランチを `main` に統一し origin を設定して push。
- リモート: `https://github.com/hirokaz-adachi/joffice-wklog.git`（branch `main`）。

---

## 12. リポジトリ構成

```
sharoshi-worklog-mvp/
├── index.html               # Topメニュー（3グループ・簡易パスワードゲート・自己完結）
├── worklog-month.html / worklog-month.js / worklog-month.css  # 工数入力（月間・PC）★日常入力の主画面
├── worklog.html / app.js / styles.css             # 工数 照会・訂正（PC・編集モーダル／日次サマリー折畳）
├── staff.html / staff.js / staff.css              # 作業登録（モバイル）
├── dashboard.html / dashboard.js / dashboard.css  # 経営ダッシュボード（モーダル含む）
├── master.html / master.js / master.css           # マスタ（スタッフ/顧客/業務区分・工程/顧客担当/設定）
├── targets.html / targets.js / targets.css         # 売上目標登録（スタッフ×月）
├── data-edit.html / data-edit.js / data-edit.css   # 請求 照会・訂正（請求billingのグリッド一括修正・請求専念）
├── analysis.html / analysis.js / analysis.css      # 詳細分析（工数の多角分析）
├── allocation.js            # 案2 配賦エンジン（ブラウザ/Node両用・時系列担当解決を共有）
├── backend.js               # JSONP APIクライアント
├── config.js                # 接続設定（.gitignore対象・各環境で作成）
├── config.sample.js         # 接続設定のプレースホルダ（コミット対象）
├── gas/
│   ├── Code.gs              # GASバックエンド（API・データモデル・TASK_CATALOG）
│   ├── DemoSeed.gs          # デモデータ再構築（rebuildDemo()）
│   ├── .clasp.json / appsscript.json  # clasp デプロイ設定
├── scripts/
│   ├── demo-dataset.mjs / verify-demo.mjs   # デモデータ参照実装＋配賦エンジン検証
│   ├── test-allocation.mjs # 配賦エンジン回帰テスト
│   ├── seed-sample-worklogs.mjs / reseed-six-staff-worklogs.mjs  # 旧サンプル投入
│   ├── deploy.ps1           # FTPSデプロイ（WinSCP）
│   ├── deploy-an2.ps1       # 案2 協調デプロイ（clasp push→setup/rebuildDemo→FTPS）
│   ├── deploy.config.ps1    # FTP認証（.gitignore対象） / deploy.config.sample.ps1
│   ├── ftp-ls.ps1           # リモート調査用
│   └── git-init.ps1 / git-init.sh / git-push-github.ps1  # git運用
├── docs/design.md           # 本資料
└── DEPLOY.md                # 配置手順
```

---

## 13. 更新履歴

### 2026-06-25（顧客マスタ正本化・デモを実顧客40社へ全面リビルド）
受領 `【DM】顧客マスタ.xlsx`「設計時確定顧客マスタ」（関与先コード `0001`形式・55社）を顧客マスタの正本化。デモ顧客を旧C001〜C022（ギリシャ文字22社）から**請求CSV出現の実顧客40社（0001形式・実名）**へ全面差し替え。
- `gas/DemoSeed.gs` `DEMO.customers`（40社）・`scripts/demo-dataset.mjs` `CUSTOMERS`（同期）・売上目標を 8名×¥480,000 に。月間税抜売上 約360〜440万。
- 名称はマスタ正本36社＋マスタ欠番4社 `0003/0009/0021/0023` は請求CSV由来（要所長確認）。生成ロジック（顧客index基準の按分・担当巡回・複数明細・時系列交代）は不変。
- `scripts/verify-demo.mjs` を顧客40社・売上レンジ 330〜460万・時系列コード 0001 に更新（30/30）。`test-allocation.mjs` は自前フィクスチャのため不変（24/24）。
- **顧客コードが請求CSVと恒等一致**となり、実請求CSV取込が同コードでマッチ（第15-3）。第4章・第8-10-1・第8-13 を更新。
- **コード列のテキスト保持を顧客コードへ拡張（バグ修正）**: `Code.gs applyCodeFormats_()` が顧客コード列（customer_master.code／billing.customerCode／worklogs.customerCode／customer_staff_master.customerCode／staff.code）を未フォーマットだったため、4桁固定の関与先コード `0001` が数値化し前ゼロが落ちていた（旧 `C001` 形式では非数値で顕在化せず）。全該当列を `@`（テキスト）化。**反映には再 `rebuildDemo()` が必要**。
- 反映には GAS 協調デプロイ（`deploy-an2.ps1`）＋ Apps Script で `rebuildDemo()` 実行が必要。

### 2026-06-25（請求CSV取込の実装：data-edit）
フロントのみ（GAS変更なし・既存 `saveBillings`/`deleteBilling` を流用）。コミット・プレビュー検証済み。
- **「請求 照会・訂正」（`data-edit.html`/`data-edit.js`/`data-edit.css`）に CSV取込を実装**。かつ・かいしゅう口座振替CSV（Shift-JIS・20列）を `FileReader.readAsArrayBuffer` ＋ `TextDecoder('shift_jis')` でデコードし、引用符対応パーサ（`app.js parseCsv` 相当）で解析。**ファイル選択 → プレビュー（確認）→ 一括 `saveBillings`** の3段。
- **取込仕様の確定（第15-3の正本に整合）**：①顧客コードはCSVの関与先コード（`0001`形式）を**そのまま保持**し、顧客名もCSVから採用。業務区分マスタ／顧客マスタ未登録は**プレビューで警告**（取込は許可）。本番は顧客マスタを関与先コード体系へ寄せる前提。②**invoiceId は決定論的** `b_{請求月}_{顧客}_{内訳}`（既存デモ `billingObj_` と同形式）で、**再取込は upsert 上書き＝重複なし**。③**消費税080は独立行・netAmount＝税額**（taxAmount=0）でデモ／`allocation.js` 踏襲。非080行は netAmount＝報酬額。④¥0行（000等）はスキップ、振替日が複数なら中断（1ファイル＝1請求月）。
- 請求月＝振替月−1を自動算出し**プレビュー上部で手修正可**。プレビューは役務／配賦対象外／未マッチ／消費税080／税抜計／税込計と警告（マッチング漏れコード・未登録顧客）を集計表示。
- 第15-3を「実装済み（2026-06-25）」に更新。

### 2026-06-25（照会・訂正のドメイン分担・各画面のUX改善・ナビ整理）
フロントのみ（GAS変更なし）。各コミット・FTPS配信・ライブ検証済み。
- **工数入力（月間）**：①メモのセル編集（ポップオーバー・複数行）②スタッフ/対象月変更で「行を追加」の顧客候補を再構築（バグ修正）③顧客ブロックの区切り（名称は先頭行のみ・境界線・交互バンド）＋行全体ホバー④**セルを表示専用化し単一クリックで編集ウィンドウ**（初期フォーカス=時間欄）⑤**複数明細セルは読取専用＋「スマホ用画面で編集」**で `staff.html` を当該日付・スタッフ・顧客/業務/工程プリセットで開く（`from=worklog-month`／戻る導線・対象月/スタッフ復元）⑥右上ナビ整理・スタッフ/月セレクトのフォント拡大。
- **工数管理 → 「工数 照会・訂正」へ再設計**（`worklog.html`/`app.js`/`styles.css`）：単票入力カード・「当日入力」・「前日コピー」を撤去し、一覧の編集／新規登録は**編集モーダル**へ。削除は確認モーダル。日次サマリーは**折り畳み＋コード基準集計＋社員番号/顧客コード併記＋行クリックで一覧フィルタ**。入力一覧にもコード併記。カレンダー日クリックで一覧を当日に絞り込み（`selectedDate` をモーダル日付と分離）。
- **データ訂正 → 「請求 照会・訂正」へ（請求専念）**（`data-edit.*`）：工数/売上タブを廃し請求グリッドのみに。工数の訂正は工数管理画面へ誘導。
- **ナビ整理**：各画面の右上ジャンプボタンを**同カテゴリの他画面＋メニュー**に統一（日常の入力は対象外）。表記を「工数 照会・訂正」「請求 照会・訂正」に統一。**メニュー（ホーム）はフィル、横移動はソフト**で差別化（`.nav-home`・styles/dashboard/worklog-month の3系統）。
- メニュー（`index.html`）の該当カード名称・説明・並びを更新。
- 第3章・第12章を本変更に整合。

### 2026-06-24（業務区分確定・デモ刷新・item_master撤去・新画面・表記統一）
本日まとめてデプロイ済み（各コミット・FTPS／GAS協調デプロイ・ライブ検証済み）。
- **工数入力（月間）新設**（`worklog-month.*`）：スタッフ1名×1か月のマトリクス入力。担当顧客シード・工程自動補完・前月構成コピー・複数明細セル読取専用保護・セルのメモ編集（ポップオーバー）。**メニュー再編**：日常の入力に本画面を追加、工数管理（`worklog.html`）を管理・設定へ移設（第3章）。
- **メモ改行対応**：全入力画面（`staff.html`/`worklog.html`/`data-edit.html`）のメモを `<input>`→`<textarea>` 化（Enter＝改行・Ctrl+Enterで登録）、表示は pre-wrap。grid は input が値の改行を除去する仕様による欠落も解消。通信(JSONP)/CSV(RFC4180)/シートは既に改行対応。
- **スマホ作業登録**：担当工程固定時の非活性セレクトを明確にグレーアウト（iOS Safari の `-webkit-text-fill-color` 対応）。
- **品目（請求項目）タブ撤去（手順1・フロント）**：`master.html`/`master.js` から item 編集を除去（第15-1）。
- **「対象外（立替）」→「配賦対象外」に表記統一**：`dashboard.js`（全社内訳・行タグ・モーダルKPI）・`master.js`（配賦区分ラベル/プルダウン/hint）・`allocation.js` コメント。027/028 追加で「立替」が不正確になるため（第8-4）。
- **業務区分カタログ確定**（第8-12）：`Code.gs TASK_CATALOG` を確定15コードへ。旧037/046/100廃止・スポット手続036一本化・027=FBデータ作成費用/028=マイナンバー管理料金（ともに配賦対象外）・002/003を「費用」付き名称に。
- **item_master 完全撤去（手順2・GAS）**：`Code.gs` の sheets/headers/MASTER_DEFS/applyCodeFormats_/bootstrap/ensureSheets から除去、`rebuildDemo()` でシート削除。bootstrap は items を返さない。
- **デモデータ再構築**（第8-13・第4章）：8名・22社・2026-04〜06・平日分散・全工数メモ・複数明細月3〜4・月間税抜約200〜250万。`scripts/demo-dataset.mjs`＋`verify-demo.mjs` で配賦エンジン検証（30/30）、ライブ `bootstrap`/`dashboard` でも実証。既存回帰 `test-allocation.mjs` 24/24 無傷。
- **本ドキュメント同期**：冒頭・第2〜5章・第8章・第10章・第12章を実態へ更新。

### 2026-06-24（ドキュメント整合：デプロイ状態を実態へ修正）
案2＋顧客担当時系列化は **2026-06-23 に協調デプロイ済み**だが、本ドキュメントが「協調デプロイ待ち／未デプロイ＝公開デモは無傷」のまま残っていたため、実態に合わせて修正（冒頭の位置づけ・最終更新、第8章状態ブロック、更新履歴2件）。ライブ `bootstrap` で `taskPhases`（30件）・`customerStaff.effectiveFrom`（C001 の 2026-05 交代行あり）・`tasks.allocationType`・`settings.billingOffset` を確認し、公開デモ反映を実証。コードは無変更。

### 2026-06-23（顧客担当の時系列化：Phase1〜5・デプロイ済み）
顧客担当が毎月変動する要件（櫻井所長）に対応し、`customer_staff_master` を有効開始月方式の時系列マスタへ。詳細は第8-10-1。**2026-06-23 に協調デプロイ済み＝公開デモに反映済み**。
- Phase1 配賦エンジン（`allocation.js`）：`resolveAssignees`/`roleAsOf`/`assigneesAsOf` を追加し、フォールバック・役割表示・主担当を**作業月時点で解決**。`effectiveFrom` 空＝従来どおりで後方互換。回帰テストに時系列5件追加（計24件）。
- Phase2 GAS/デモ（`gas/Code.gs`・`gas/DemoSeed.gs`）：`customer_staff_master` に `effectiveFrom` 列追加（末尾・テキスト保持）。保存APIは `(顧客,役割,有効開始月)` upsert＋解除許容、削除は同キー。bootstrap/dashboard が `effectiveFrom` を返却。デモは C001 の Prepare 担当を 2026-05 から交代。
- Phase3 入力/訂正画面（`app.js`/`staff.js`/`data-edit.js`）：`roleFor`/`groupedCustomers` を作業月基準でエンジン解決へ委譲。作業日/行日付の変更で顧客並び・工程補完/不一致警告を再構築。各HTMLに `allocation.js` 読込。
- Phase4 マスタ画面（`master.js`）：顧客担当タブに月セレクタ＋「その月から有効」編集（年月指定の照会兼）。差分のみ保持、`当月取消`、`この月〜`/`引継` タグ。`deleteCustomerStaff` を `(顧客,役割,有効開始月)` キーに（`backend.js`）。
- Phase5 本ドキュメント更新。協調デプロイ手順は従来どおり（clasp push → `rebuildDemo()` → FTPS）。

### 2026-06-23（案2 実装：GASデモ・デプロイ済み）
設計確定（同日・下記）に続けて案2を Phase1〜4 実装。**2026-06-23 に協調デプロイ済み＝公開デモに反映済み**（`scripts/deploy-an2.ps1`：clasp push → `setup()` → `rebuildDemo()` → FTPS）。
- Phase1 データモデル/API（`gas/Code.gs`）：新シート `task_phase_master`／`customer_staff_master`／`app_settings`、worklogs に `taskCode`/`phaseCode`、billing に `invoiceItemCode`/`transferDate`、task_master を code＋name＋配賦区分へ。品目カタログ seed（`seedTaskCatalog_`）。工程／顧客担当／設定の更新系API（`backend.js` クライアントも追加）。
- Phase2 デモ再生成（`gas/DemoSeed.gs` `rebuildDemo()`）：合成3ヶ月・各ケース網羅（通常按分・Review枠/全額フォールバック・諸費用・消費税・社内工数）。
- Phase3 配賦エンジン（`allocation.js`・ブラウザ/Node両用）：第8章アルゴリズム。`scripts/test-allocation.mjs` 回帰テスト合格（Σ帰属＝役務売上 等）。
- Phase4 UI：ダッシュボード（役務/総売上・消費税・帰属売上・時間単価＝工数対応・主担当＝マスタ・未記録・達成率＝役務基準）、詳細分析（指標切替 工数/帰属売上・時間単価・売上サマリ）、マスタ（業務区分＋配賦区分＋工程比・顧客担当・設定オフセット）、工数入力PC/モバイル（業務区分コード化・工程セレクタ・社内工数）、data-edit（taskCode/phaseCode・invoiceItemCode/transferDate）。
- 全HTMLの `backend.js?v` 更新、`deploy.ps1` 配信対象に `allocation.js` 追加。
- 協調デプロイ手順：clasp push（Code.gs/DemoSeed.gs）→ Apps Scriptで `setup()`→`rebuildDemo()` → FTPS で全Webファイル配信。

### 2026-06-23（案2 設計確定・第8章全面改訂）
受領資料 `J-Office_Insight_管理/【DM】請求品目一覧.xlsx`（業務コード・Prepare/Review比・名称）を反映し、第8章を「工程対応・顧客担当者フォールバック配賦」として全面改訂（設計確定・未実装）。主な確定事項：
  - 紐づけは **業務コード基準の恒等マッチング**（billing.invoiceItemCode＝task.code＝worklog.taskCode）。請求側の業務コードがそのまま業務区分で、工程比・名称も同表に載るため、別マッピング列は不要（1:1）。
  - **工程（Prepare/Review）** は全業務区分が振分率を持つ統一モデル（分割なしは Prepare:100/Review:0）。N工程・任意振分率に拡張可能（当面2固定）。工数側は `phaseCode` を別カラム保持。
  - **配賦区分（役務／対象外）** を新設。0/0 の品目を性質で切り分け：消費税(080)・諸費用(060)＝対象外（スタッフ配賦せず、工数登録対象外、総売上にのみ計上）。027 は仮・対象外（所長回答待ち）。
  - フォールバックは **役割対応**（Review枠の未計上→Review担当へ）。`customer_staff_master.role` は工程コードを指す。人事オフィスは1顧客=Prepare/Review各1名固定だが複数担当を許容。
  - **作業→請求オフセット**（当月/翌月/翌々月＝0/1/2）をグローバル設定。マッチングは「請求月B↔作業月 B−offset」。
  - 売上を **配賦売上（金額系）／工数対応売上（per-hour系）** の2系統に分離。per-hour系は顧客個別も含め工数対応売上に統一。**売上目標は役務売上基準**（Σスタッフ達成率＝全社達成率が一致）。総売上＝役務売上＋配賦対象外を別建て表示。フォールバック分は達成率に反映、対象外分は反映しない。
  - データモデル変更（task_master 拡張＋配賦区分・task_phase_master/customer_staff_master 新設・worklogs に taskCode/phaseCode・billing_data に invoiceItemCode・settings にオフセット）。実装時にデモデータを作り直す。
  - 所長回答待ち：027配賦要否・028/036/037用途差・000/057正体・消費税のCSV持ち方・他コード有無（第8-14）。
- 関連して第7-1・7-2、第14章の案2記述、冒頭の位置づけ／最終更新を本設計に整合。
- 追記（同日・受領請求CSV確認）: `【マスク済】202603末〆.csv`（かつ・かいしゅう口座振替CSV・Shift-JIS）を確認。[8]内訳＝業務コードを実証（未知コードなし）。**消費税(080)は非080税抜合計の10%の独立税行**＝売上でなく税として扱い税抜売上から除外（第8-4・8-14解決済）。**請求月＝振替日の前月（振替月−1）**、振替日も保持（第8-5(ア)）。(イ)作業オフセットは別軸・基本0。第15-3に列マッピングと取込ルールを確定。

### 2026-06-22（詳細分析画面・工数フェーズ）
- 画面追加：**詳細分析**（`analysis.html`／`analysis.js`／`analysis.css`）。任意期間でスタッフ／顧客／業務区分を軸に工数を分析（①月次推移スタックチャート ②ランキング/構成比 ③クロス集計ピボット）。集計期間はアンカー月起点の連続月から選択でき、データのない月はゼロ表示。Topメニュー（`index.html`）とダッシュボードのナビにリンク追加。第3章の画面表に追加、第15-2 を工数フェーズ実装済みに更新。
- 方針：データは `dashboard` API 再利用で **GAS変更なし**。売上系指標（帰属売上・時間単価・達成率）は配賦の非対称（第7-1）の影響を避け、案2（第8章）決着後に同画面へ追加。ベースCSSは `dashboard.css` を再利用し分析専用は `analysis.css`。
- 運用：`scripts/deploy.ps1` の配信ホワイトリスト（`$webFiles`）に analysis 3ファイルを追加。アセット参照は `?v=` キャッシュバスター付与。
- 整理：工数管理画面（`worklog.html`）内のスタッフ／顧客マスタ編集セクションを撤去（機能は `master.html` へ切り出し済みのため）。`app.js`／`styles.css` から該当UI・ハンドラ・スタイルを削除（入力フォーム・フィルタのマスタ選択肢取得は維持）。`styles.css`／`app.js` の `?v=` を更新。

### 2026-06-22（実装反映）
- 画面追加：マスタメンテナンス（`master.html`・スタッフ／顧客／業務区分／品目の4マスタをタブ編集）、売上目標登録（`targets.html`・スタッフ×月・前月コピー・一括保存）、データ訂正・編集（`data-edit.html`・工数/請求をExcelライクなグリッドで期間/スタッフ/顧客フィルタ＋行追加削除＋一括保存）。これにより第15-1・第15-4は概ね実装済みに更新。
- データモデル：`item_master`（品目コード表）を追加し6→**7シート**化。`bootstrap` は items も返す（未作成シートは安全に空配列）。
- API：請求（`saveBilling`/`saveBillings`/`deleteBilling`）と売上目標（`saveTarget`/`saveTargets`/`deleteTarget`）の更新系を追加。`upsertMaster`/`removeMaster` の対応typeを staff/customers/tasks/items の4種へ拡張。
- 運用：GASバックエンドを clasp でデプロイ可能に（`gas/.clasp.json`・`gas/appsscript.json` 追加。`appsscript.json` の `webapp` セクション必須＝欠落するとexec URLが停止）。`backend.js` を `?v=` キャッシュバスター対象に追加。
- 補足：本エントリ時点で残課題としていた第15-2（詳細分析画面）は、同日中に着手し上記「2026-06-22（詳細分析画面・工数フェーズ）」のとおり工数フェーズを実装済み。案2（売上配賦の精緻化・第8章）は引き続き保留中。

### 2026-06-22（バックログ整理）
- 現状デモがFIX版でない旨を第1章に明記。デモ未実装の本番実装スコープを第15章として新設（マスタ独立画面化＋拡張／スタッフ別詳細分析・集計期間の動的設定／請求CSV取込／集計データ訂正編集）。既存バックログ（第14章）から第15章へ相互参照。

### 2026-06-18
- スタッフ月次サマリ（モーダル）：「帰属売上が大きい上位5社」から「当月の全工数内訳（顧客＋社内/非生産工数・工数の多い順、社内は末尾）」へ変更。内訳合計工数＝総工数で一致。
- 顧客別時間単価表：行クリックで顧客プロファイル・モーダルを追加。時間単価の12ヶ月推移チャート（欠損月は0・単価帯で色分け）、当月KPI、当月の関与スタッフ内訳、当月の請求内訳を表示。推移バー（列）クリックでモーダル内の表示月のみ切替（ダッシュボード対象月・チャートは不変）。
- 運用基盤：FTPSデプロイスクリプト（`scripts/deploy.ps1`）と git 初期化／GitHub プッシュスクリプトを追加。`config.js`・`deploy.config.ps1` は `.gitignore` で秘匿。
- 画面構成変更：`index.html` を工数管理から**Topメニュー**に変更し、工数管理画面は `worklog.html` へ移設。Topメニューにデモ用の簡易パスワードゲート（`sessionStorage` 保持）を追加。
- 製品名を **J-Office Insight** に統一（全画面のヘッダー表記・ページタイトル）。
- 未了：案2（主担当フォールバック配賦＋売上2系統化）は保留中（第8章）。本番認証・DB（MySQL）方針は production-auth-db-memo.md。


---

## 14. 本番向け未着手事項（バックログ）

本番提供に向けて未着手の項目を集約する。優先度・規模は別途検討。

- ~~**売上配賦の精緻化（案2）**~~：**2026-06-23 実装・デプロイ済み**。業務コード基準の恒等マッチング＋工程（Prepare/Review）対応、工数の裏付けが無い売上は顧客担当者マスタへ役割対応フォールバック、売上を「配賦売上（達成率用）／工数対応売上（時間単価用）」の2系統に分離。業務区分カタログは 2026-06-24 確定。設計・現行仕様は第8章。
- **認証・DB移行**：スタッフ工数登録の社員番号＋パスワード認証、および GAS＋スプレッドシート → PHP＋MySQL への移行。詳細は [production-auth-db-memo.md](./production-auth-db-memo.md)。
- **ダッシュボード表示範囲の可変指定**：現在は当月終端の固定12ヶ月。表示期間（例: 3/6/12ヶ月や任意期間）をユーザーが指定できるようにする。（→15-2 スタッフ別詳細分析の集計期間動的設定に統合）
- **各カードのリスト高さ固定（スクロール表示）**：スタッフ別・顧客別などのリストを固定高さ＋カード内スクロールにし、件数によらずページ全体のレイアウトを安定させる。
- **マスタメンテナンスカードのモーダル化**：工数管理画面のスタッフ/顧客マスタ編集をモーダルへ移し、入力画面をすっきりさせる。（→15-1 マスタ独立画面化＋拡張に統合・実装済み）
- **スタッフ毎目標売上登録画面の追加**：`targets.html` として実装済み（→15-1 売上目標登録に統合）。


---

## 15. デモ未実装機能（本番実装スコープ）

前提: 現状のデモ版はFIX版ではない。以下はデモに未実装で、本番で新規実装する機能。マスタの種類・項目をはじめ要件は今後さらに増える可能性があるため、拡張に耐える設計を前提とする。

### 15-1. マスタメンテナンスの独立画面化＋マスタ拡張 【概ね実装済み（2026-06-22）】
- 当初の現状: 工数管理画面（`worklog.html`）内にスタッフ／顧客マスタ編集が同居。`task_master`（業務区分）・`staff_target_master`（売上目標）はデータのみ存在し編集UIがない。
- 仕様:
  - メニューに「マスタメンテナンス」を**独立画面**として追加。→ **`master.html` で実装済み**。
  - 扱うマスタ：スタッフ、顧客、業務区分・工程（`task_master`＋`task_phase_master`・配賦区分＋工程比）、顧客担当（`customer_staff_master`・時系列）、設定（`app_settings`・オフセット）。売上目標は独立画面 `targets.html`（スタッフ×月・前月コピー・一括保存）。→ **`master.html` でタブ編集（実装済み）**。
  - **品目（請求項目）マスタ（`item_master`）は 2026-06-24 撤去**。案2の恒等マッチングで請求項目コードの正本が業務区分マスタ（`task_master`）に移り不要となったため（第8-2・第4章）。フロント（品目タブ）と GAS の双方から除去済み。
  - **マスタの種類・項目の追加は今後も発生する前提**とし、拡張しやすい構造にする。
- 旧バックログ「マスタメンテナンスのモーダル化」「スタッフ毎目標売上登録画面」は本項に統合。
- **工数管理画面（`worklog.html`）内のスタッフ／顧客マスタ編集セクションは撤去済み（2026-06-22）**。マスタ編集は `master.html` に一本化。
- **残課題**: 顧客の主担当は `customer_staff_master`（顧客担当タブ）で管理する形に確定済み（案2）。品目マスタ連携の課題は item_master 撤去により解消。

### 15-2. 詳細分析画面（時系列・集計期間の動的設定）【工数フェーズ実装済み（2026-06-22）】
- 当初の現状: スタッフ詳細はモーダルで「当月」のみ。
- 仕様:
  - ダッシュボードから遷移する**スタッフ別の詳細分析画面**を新設。
  - 年間を通じた時系列での推移分析（売上・工数・時間単価・達成率などの月次推移）。
  - **集計期間の動的設定を必須要件**とする（例：3〜9月の売上合計、任意の開始月〜終了月での集計・比較）。
- 旧バックログ「ダッシュボード表示範囲の可変指定」と整合。
- **進め方（2026-06-22 合意）**: スタッフ別に限定せず、スタッフ・顧客・業務区分などを軸に切り替えて掘る「詳細分析画面」として一般化する。**工数中心の分析を先行実装**（推移・ランキング・クロス集計まで）。売上系指標（帰属売上・時間単価・達成率）は配賦の非対称（第7-1章）の影響を受けるため、**案2（第8章）の決着後に同画面へ追加**する。データは既存 `dashboard` API を再利用しクライアント側集計（GAS変更なし）。詳細は `docs/handoff-code-2026-06-22.md`。
- **実装内容（工数フェーズ・2026-06-22 デプロイ）**: `analysis.html`／`analysis.js`／`analysis.css` を新設。Topメニューとダッシュボードのナビからリンク。
  - 集計期間: 任意の開始月〜終了月。選択肢はアンカー月（直近データ月 or 当月）起点の連続月で、**データのない月もゼロとして選択可**（始端はデータ最古月と直近12ヶ月窓の早い方。データが増えれば過去月も自動で選択肢に出る）。
  - 集計軸: スタッフ／顧客／業務区分の切替。社内・非生産工数（customerCode空欄）の含む/除く切替。
  - 出力: ①月次工数推移（選択軸でスタックした自前描画チャート・上位8系列＋その他）②工数ランキング/構成比（合計・構成比・月平均）③クロス集計ピボット（行軸×列軸の工数・ヒート表示）。
  - 注: ダッシュボードのブラウザキャッシュ（`worklog-dashboard-cache-v2`）は taskType を捨てるため相乗りせず、業務区分軸を成立させるため毎回フレッシュ取得（GAS側キャッシュに依存）。
- **残（売上フェーズ）**: 帰属売上・時間単価・達成率の軸は案2（第8章）決着後に同画面へ追加。

### 15-3. 売上（請求）データのCSV取り込み 【実装済み（2026-06-25・data-edit）】
- 現状: 工数のCSV入出力はあるが、請求（`billing_data`）は取込UIがなく、スプレッドシートへ手入力。
- 仕様: 「かつ・かいしゅう」の口座振替CSV（Shift-JIS）を取り込むボタンを追加し、`billing_data` へマッピング取込。
- **CSV構造（2026-06-23 受領 `【マスク済】202603末〆.csv` で確認）**: 20列・1行＝1関与先×1内訳。1ファイル＝1請求月（振替日単一）。
- **列マッピング**:

| billing_data | CSV列 | 備考 |
|---|---|---|
| customerCode | [6] 関与先コード | 例 0001 |
| customer | [7] 関与先名 | |
| invoiceItemCode | [8] 内訳 | ＝業務コード（恒等マッチング） |
| netAmount（税抜） | [9] 報酬額 | 内訳≠080。源泉控除前の報酬額を採用 |
| 消費税 | [9] 報酬額（内訳=080行） | **税**として分離。税抜売上に含めない。税込算出のみ。080行を正（端数は再計算しない） |
| transferDate | [0] 振替日 | 保持 |
| billingMonth（請求月） | （導出）振替月 − 1 | 取込時に確定・手修正可 |
| 源泉税額 | [10] 源泉税額 | 保持（当月は全0） |

- **取込ルール**: ¥0行（コード000等）はスキップ。未知の業務コードはマッチング漏れ警告（第8-11）。計上基準＝請求月（振替月−1）。振替日が複数のファイルは中断（1ファイル＝1請求月）。
- **格納（実装で確定・2026-06-25）**: `invoiceId` は決定論的 `b_{請求月}_{顧客}_{内訳}`（既存デモ `billingObj_` と同形式）。**再取込は upsert 上書き＝重複なし**。消費税080は**独立行・`netAmount`＝税額**（`taxAmount`=0）で保存し、`allocation.js` が税として集計（税抜には含めない）。顧客コードはCSVの関与先コードを**そのまま保持**（顧客マスタ未登録はプレビュー警告のみ・自動追加しない／本番は顧客マスタを関与先コード体系へ寄せる前提）。
- 関連: 受領データ依頼・QA No.7。**実装済み（`data-edit`・第13章 2026-06-25）**。所長回答待ち項目は第8-14。

### 15-4. 集計データ訂正・編集画面（Excelライク） 【実装済み（2026-06-22）】
- 当初の現状: 工数は一覧からの行単位編集・削除のみ、売上は手入力。一括修正UIがない。
- 仕様: 登録済みの工数・売上データを、**Excelライクなグリッドで一括修正**できるメンテナンス画面を新設。CSV取込後の補正や誤入力修正を想定。
- 実装: **`data-edit.html` で実装済み**（2026-06-22）。当初は工数（worklogs）と請求（billing_data）をタブ切替で編集していたが、**2026-06-25 に照会・訂正をドメインで分担**し、`data-edit.html` は**請求（billing）専念**（「請求 照会・訂正」）に。工数の照会・訂正は `worklog.html`（「工数 照会・訂正」・編集モーダル）が担う。請求は請求月/顧客でフィルタ、行追加／削除、変更行のみ一括保存（`saveBillings`／`deleteBilling`）。
